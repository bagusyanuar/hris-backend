---
description: Trigger the auto-commit AI skill to group and commit changes.
---

# Git Commit Workflow

When this workflow is triggered (via `/git-commit`), you MUST read and follow the instructions defined in the `auto-commit` skill located at `.agents/skills/auto-commit/SKILL.md`.

Execute the full auto-commit process (status check, unstaging if needed, grouping, changelog generation, and atomic commits) as described in that skill.
